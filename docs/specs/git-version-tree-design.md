# Git 版本管理树（已并入《仓库内 Git 模式》）

> **本文已合并**：可视化子设计（提交 DAG / 引用树 / 文件历史、泳道布局算法、分页与性能口径、
> 引擎接口清单、落地阶段）全部并入 [`git-mode-design.md`](git-mode-design.md) ——
> 那份现在是 **Git 模式唯一的开发进度与设计文档**（2026-09 收束）。
>
> 保留本文件（而不是删掉）只有一个原因：旧提交信息与历史文档里的链接不能变成死链。
> **新内容一律写进 `git-mode-design.md`**，不要再往这里加东西。
>
> 原内容对应的章节：三视图与画法 → `git-mode-design.md` §4；
> 泳道布局算法与 `CommitGraphLayout` → §4.2；数据源分层与「不设上限」→ §4.3；
> 引擎接口 → §7；分阶段落地 → §8；性能与验收 → §10。
